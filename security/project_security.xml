<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Record Rule: Project Users can create stages for their accessible projects -->
        <record id="project_task_type_rule_project_user" model="ir.rule">
            <field name="name">Project User: Task Type Access</field>
            <field name="model_id" ref="project.model_project_task_type"/>
            <field name="domain_force">[
                '|',
                ('project_ids', '=', False),
                ('project_ids', 'in', user.partner_id.project_ids.ids)
            ]</field>
            <field name="groups" eval="[(4, ref('project.group_project_user'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="False"/>
        </record>

        <!-- Additional security rule to ensure users can manage stages in their projects -->
        <record id="project_stage_create_rule" model="ir.rule">
            <field name="name">Project User: Create Stages in Accessible Projects</field>
            <field name="model_id" ref="project.model_project_task_type"/>
            <field name="domain_force">[
                '|',
                    ('create_uid', '=', user.id),
                    '|',
                        ('project_ids', '=', False),
                        ('project_ids.user_id', '=', user.id)
            ]</field>
            <field name="groups" eval="[(4, ref('project.group_project_user'))]"/>
        </record>
    </data>
</odoo>

