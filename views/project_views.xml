<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Optional: Add a menu item for Project Users to manage stages -->
    <record id="action_project_task_type_user" model="ir.actions.act_window">
        <field name="name">Project Stages</field>
        <field name="res_model">project.task.type</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first project stage
            </p>
            <p>
                Stages help you organize your project tasks and track their progress.
            </p>
        </field>
    </record>

    <!-- Menu item for Project Users -->
    <menuitem
        id="menu_project_task_type_user"
        name="Stages"
        parent="project.menu_project_config"
        action="action_project_task_type_user"
        groups="project.group_project_user"
        sequence="5"/>

    <!-- Optional: Extend project form view to show stage management -->
    <record id="project_project_view_form_inherit_stage_mgmt" model="ir.ui.view">
        <field name="name">project.project.form.stage.management</field>
        <field name="model">project.project</field>
        <field name="inherit_id" ref="project.edit_project"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button class="oe_stat_button" type="action"
                        name="%(project.project_task_type_action)d"
                        icon="fa-tasks"
                        groups="project.group_project_user">
                    <field string="Stages" name="type_count" widget="statinfo"/>
                </button>
            </xpath>
        </field>
    </record>
</odoo>

